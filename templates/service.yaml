apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-composite-values.fullname" . }}
  labels:
    {{- include "chart-composite-values.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-composite-values.selectorLabels" . | nindent 4 }}
