apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-composite-values.fullname" . }}-test-connection"
  labels:
    {{- include "chart-composite-values.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-composite-values.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
